{
    adminName: "{{ users.primary.user }}",
    adminPassword: "{{ cloudbeaver.password }}",

    roles: [
        {
            roleId: "admin",
            name: "Admin",
            description: "Administrative access. Has all permissions.",
            permissions: [ "public", "admin" ]
        }
    ]
}
