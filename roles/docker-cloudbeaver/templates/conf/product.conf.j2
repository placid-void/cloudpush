{
    // Global properties
    core: {
        // User defaults
        user: {
            defaultTheme: "dark",
            defaultLanguage: "en"
        }
    },
    // Log viewer config
    logViewer: {
        refreshTimeout: 3000,
        logBatchSize: 1000,
        maxLogRecords: 2000
    }
}
