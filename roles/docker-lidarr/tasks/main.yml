---
- name: Create DNS Reconds on Cloudflare for this container
  cloudflare_dns:
    account_api_token: "{{ cloudflare.api }}"
    account_email: "{{ cloudflare.email }}"
    proxied: "yes"
    record: "{{ containers.lidarr.subdomain }}"
    solo: "true"
    state: "present"
    type: "{{ item.type }}"
    value: "{{ item.value }}"
    zone: "{{ domain }}"
  loop:
    - { type: 'A', value: '{{ ansible_default_ipv4.address }}' }
    - { type: 'AAAA', value: '{{ ansible_default_ipv6.address }}' }

- name: Verify required directories with correct permissions are created
  file:
    recurse: yes
    path: "{{ directories.container_root }}/{{ containers.lidarr.subdomain }}"
    owner: "{{ users.primary.user }}"
    group: "{{ users.primary.group }}"
    state: directory
    setype: container_file_t

- name: Verify templated required files with correct permissions are created
  template:
    src: "templates/config.xml.j2"
    dest: "{{ directories.container_root }}/{{ containers.lidarr.subdomain }}/config.xml"
    owner: "{{ users.primary.user }}"
    group: "{{ users.primary.group }}"
    mode: 0660

- name: Start Lidarr
  docker_container:
    name: "{{ containers.lidarr.subdomain }}"
    hostname: "{{ containers.lidarr.subdomain }}"
    state: started
    restart_policy: unless-stopped
    image: linuxserver/lidarr:latest
    links: # Prevent container creation and startup until linked containers are running
    - "{{ containers.nzbget.subdomain }}:{{ containers.nzbget.subdomain }}"
    log_driver: "json-file"
    log_options:
      max-size: "12m"
      max-file: "3"
    volumes: # Mount required directories with the container
      - "{{ directories.container_root }}/{{ containers.lidarr.subdomain }}:/config:z"
      - "{{ directories.host_data_dir }}:{{ directories.data_dir }}:shared"
    env:
      PUID: "{{ users.primary.uid }}" # Pass proper UID to the container
      PGID: "{{ users.primary.gid }}" # Pass proper GID to the container
      UMASK: "{{ mount_options.umask }}" # File UMASK
    networks_cli_compatible: no
    networks:
      - name: "{{ networks.docker.primary.name }}"
        aliases: "{{ containers.lidarr.subdomain }}"
    purge_networks: yes
    pull: true
    labels:
      ## Watchtower Updates
      com.centurylinklabs.watchtower.enable: "true"
      ## Enable Traefik
      traefik.enable: "true"
      ## HTTP Routers
      traefik.http.routers.lidarr-rtr.entrypoints: "https"
      traefik.http.routers.lidarr-rtr.rule: "Host(`{{ containers.lidarr.subdomain }}.{{ containers.lidarr.tld }}`)"
      traefik.http.routers.lidarr-rtr.tls: "true"
      ## HTTP Services
      traefik.http.routers.lidarr-rtr.service: "lidarr-svc"
      traefik.http.services.lidarr-svc.loadbalancer.server.port: "{{ containers.lidarr.port | default('80') }}"
      ## Middlewares
      traefik.http.routers.lidarr-rtr.middlewares: "chain-oauth@file"
...
